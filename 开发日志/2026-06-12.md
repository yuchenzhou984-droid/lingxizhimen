# 开发日志 - 2026-06-12

## 完成事项

### 首页视频背景系统重构
- 视频改为直接放在 body 下，移除包裹层，解决点击拦截问题
- `#app` 默认 `display:none`，修复深色背景遮挡视频的Bug
- body/html 设为透明背景，视频全屏无遮挡
- 排查过程：逐个添加CSS/HTML元素定位根因

### 视频过渡优化
- 替换为第二版视频背景（第二版.mp4）
- 视频结束后1.2s渐变切换到高清背景图（背景.webp）
- 新增满屏金光过渡特效（2秒金色光晕脉冲动画）
- 音频结尾淡出从3秒改为2秒
- 待接入过渡音效（等用户提供音效文件）

### 首页卡片系统
- 3张淡紫毛玻璃卡片（能量测试/魔杖定制/塔罗占卜）
- 卡片3D倾斜动效：鼠标靠近时向光标方向倾斜+微放大
- 淡入动画改用 `margin-top`，避免与倾斜 `transform` 冲突
- 视频播放期间无任何标签/元素
- 视频结束后标题→副标题→卡片依次淡入

### Bug修复
- 修复 `testContainer` ID在两测试页面重复导致test2空白
- 修复 sed 替换误删 `this.` 前缀和 `testTitle` 变量
- 修复返回首页无内容（首页移出 `#app`）
- 修复 `#app` z-index遮挡视频（改为默认隐藏）

### 测试页排查记录
- test_clean.html：纯CSS，按钮可点 → CSS没问题
- test_with_js.html：CSS+JS，按钮可点 → JS没问题
- video_test.html：纯视频，播放正常 → 视频文件没问题
- css_test1~3.html：逐步加CSS，视频正常 → CSS不遮挡视频
- 最终定位：`#app`的深色背景+min-height遮挡视频

## 当前状态

| 模块 | 状态 | 备注 |
|------|------|------|
| 首页视频+过渡 | ✅ | 第二版视频→金光→背景图 |
| 首页卡片 | ✅ | 3卡片+倾斜动效+依次淡入 |
| 声音开关 | ✅ | 毛玻璃按钮 |
| 返回不重播 | ✅ | 直接显示背景图+卡片 |
| 能量测试 | ✅ | 14题加权计分 |
| 魔杖测试 | ✅ | 7题加权计分 |
| 塔罗占卜 | ✅ | 78牌+4牌阵 |
| 壁纸生成 | ✅ | Canvas绘制 |
| 过渡音效 | ⬜ | 等用户提供音效文件 |
| Gitee部署 | ⬜ | 待开始 |

## 文件变动

- `index.html` — 简化视频结构，新增背景图层+金光层
- `css/home.css` — 重写视频背景样式，新增金光动画
- `css/global.css` — html/body背景透明，`#app`默认隐藏
- `js/app.js` — 视频淡出+背景图淡入+金光触发
- `js/router.js` — 适配新结构，简化导入
- `js/test-engine.js` — 修复test2空白Bug，清理调试代码
